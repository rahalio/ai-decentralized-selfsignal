/**
 * ConsentGrantRepositoryAdapter - Port Adapter Implementation
 *
 * Generated by Port Adapter Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: consent-grants
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ConsentGrantRepositoryAdapter
 *
 * DDD: Infrastructure adapter that implements ConsentGrantRepository port.
 * Handles infrastructure concerns (HTTP, database, external APIs).
 * Encapsulates Ddb creation - api-server passes dynamoClient only.
 */

import type {
  ConsentGrantRepository,
} from "@selfsignal/services/consent-grants";
import type { AdapterDynamoDBClient } from "../_shared/dynamodb-client-types.js";
import { ConsentGrantRepositoryDdb } from "./consent-grant-repository.ddb.js";

/**
 * ConsentGrantRepositoryAdapter
 *
 * Implements ConsentGrantRepository port for infrastructure layer.
 */
export class ConsentGrantRepositoryAdapter implements ConsentGrantRepository {
  private readonly ddb: ConsentGrantRepositoryDdb;

  constructor(
    private readonly dynamoClient: AdapterDynamoDBClient
  ) {
    this.ddb = new ConsentGrantRepositoryDdb(this.dynamoClient);
  }

  async listConsentGrants(input: Parameters<ConsentGrantRepository['listConsentGrants']>[0]): Promise<Awaited<ReturnType<ConsentGrantRepository['listConsentGrants']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.listConsentGrants(input);
  }

  async createConsentGrant(input: Parameters<ConsentGrantRepository['createConsentGrant']>[0]): Promise<Awaited<ReturnType<ConsentGrantRepository['createConsentGrant']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.createConsentGrant(input);
  }

  async getConsentGrant(input: Parameters<ConsentGrantRepository['getConsentGrant']>[0]): Promise<Awaited<ReturnType<ConsentGrantRepository['getConsentGrant']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.getConsentGrant(input);
  }
}
