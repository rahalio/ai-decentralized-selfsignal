/**
 * GetSignalActivation handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: signal-activations
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getSignalActivation } from "/api-server/domains/signal-activations/handlers";
import type { SignalActivationsDDDDependencies } from "/api-server/domains/signal-activations";
import { createSignalActivation as createSignalActivationEntity } from "/factories/signal-activations";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "/tests/shared";
import { SIGNALACTIVATIONS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetSignalActivation", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: SignalActivationsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<SignalActivationsDDDDependencies>(
      SIGNALACTIVATIONS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createSignalActivationEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetSignalActivation, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { activationId: entity.id };

    await getSignalActivation(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetSignalActivation.execute).toHaveBeenCalledWith(expect.objectContaining({ activationId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { activationId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetSignalActivation, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getSignalActivation(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
