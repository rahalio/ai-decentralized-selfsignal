/**
 * RunCampaignComplianceGate handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: campaigns
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { runCampaignComplianceGate } from "/api-server/domains/campaigns/handlers";
import type { CampaignsDDDDependencies } from "/api-server/domains/campaigns";
import { createCreateComplianceGateRequest } from "/factories/campaigns";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "/tests/shared";
import { CAMPAIGNS_USE_CASES } from "../__factories__/use-cases.js";

describe("RunCampaignComplianceGate", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: CampaignsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<CampaignsDDDDependencies>(
      CAMPAIGNS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = createCreateComplianceGateRequest();
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeRunCampaignComplianceGate, "execute").mockResolvedValue(useCaseResult);

    await runCampaignComplianceGate(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeRunCampaignComplianceGate.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
