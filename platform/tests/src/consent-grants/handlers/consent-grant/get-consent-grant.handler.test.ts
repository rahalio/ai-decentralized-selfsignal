/**
 * GetConsentGrant handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: consent-grants
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getConsentGrant } from "/api-server/domains/consent-grants/handlers";
import type { ConsentGrantsDDDDependencies } from "/api-server/domains/consent-grants";
import { createConsentGrant as createConsentGrantEntity } from "/factories/consent-grants";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "/tests/shared";
import { CONSENTGRANTS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetConsentGrant", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ConsentGrantsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ConsentGrantsDDDDependencies>(
      CONSENTGRANTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createConsentGrantEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetConsentGrant, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { grantId: entity.id };

    await getConsentGrant(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetConsentGrant.execute).toHaveBeenCalledWith(expect.objectContaining({ grantId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { grantId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetConsentGrant, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getConsentGrant(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
