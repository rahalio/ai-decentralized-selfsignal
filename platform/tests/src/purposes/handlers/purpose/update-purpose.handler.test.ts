/**
 * UpdatePurpose handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: purposes
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { updatePurpose } from "/api-server/domains/purposes/handlers";
import type { PurposesDDDDependencies } from "/api-server/domains/purposes";
import { createPurpose as createPurposeEntity } from "/factories/purposes";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "/tests/shared";
import { PURPOSES_USE_CASES } from "../__factories__/use-cases.js";

describe("UpdatePurpose", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: PurposesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<PurposesDDDDependencies>(
      PURPOSES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = createPurposeEntity({ orgId });
    const input = {} as any;
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { purposeId: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeUpdatePurpose, "execute").mockResolvedValue(useCaseResult);

    await updatePurpose(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeUpdatePurpose.execute).toHaveBeenCalledWith(expect.objectContaining({ purposeId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
