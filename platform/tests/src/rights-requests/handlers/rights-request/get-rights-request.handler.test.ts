/**
 * GetRightsRequest handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: rights-requests
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getRightsRequest } from "/api-server/domains/rights-requests/handlers";
import type { RightsRequestsDDDDependencies } from "/api-server/domains/rights-requests";
import { createRightsRequest as createRightsRequestEntity } from "/factories/rights-requests";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "/tests/shared";
import { RIGHTSREQUESTS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetRightsRequest", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: RightsRequestsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<RightsRequestsDDDDependencies>(
      RIGHTSREQUESTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createRightsRequestEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetRightsRequest, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { requestId: entity.id };

    await getRightsRequest(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetRightsRequest.execute).toHaveBeenCalledWith(expect.objectContaining({ requestId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { requestId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetRightsRequest, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getRightsRequest(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
