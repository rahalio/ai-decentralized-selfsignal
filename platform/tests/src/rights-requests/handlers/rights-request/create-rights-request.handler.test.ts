/**
 * CreateRightsRequest handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: rights-requests
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { createRightsRequest } from "/api-server/domains/rights-requests/handlers";
import type { RightsRequestsDDDDependencies } from "/api-server/domains/rights-requests";
import { createRightsRequest as createRightsRequestEntity } from "/factories/rights-requests";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "/tests/shared";
import { RIGHTSREQUESTS_USE_CASES } from "../__factories__/use-cases.js";

describe("CreateRightsRequest", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: RightsRequestsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<RightsRequestsDDDDependencies>(
      RIGHTSREQUESTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = {} as any;
    const entity = createRightsRequestEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeCreateRightsRequest, "execute").mockResolvedValue(useCaseResult);

    await createRightsRequest(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeCreateRightsRequest.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(201);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
