/**
 * EraseVaultProfile handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: vault-profiles
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { eraseVaultProfile } from "/api-server/domains/vault-profiles/handlers";
import type { VaultProfilesDDDDependencies } from "/api-server/domains/vault-profiles";
import { createCreateEraseRequest } from "/factories/vault-profiles";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "/tests/shared";
import { VAULTPROFILES_USE_CASES } from "../__factories__/use-cases.js";

describe("EraseVaultProfile", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: VaultProfilesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<VaultProfilesDDDDependencies>(
      VAULTPROFILES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = createCreateEraseRequest();
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeEraseVaultProfile, "execute").mockResolvedValue(useCaseResult);

    await eraseVaultProfile(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeEraseVaultProfile.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
