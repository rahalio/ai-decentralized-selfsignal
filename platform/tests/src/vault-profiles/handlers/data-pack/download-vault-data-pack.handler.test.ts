/**
 * DownloadVaultDataPack handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: vault-profiles
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { downloadVaultDataPack } from "/api-server/domains/vault-profiles/handlers";
import type { VaultProfilesDDDDependencies } from "/api-server/domains/vault-profiles";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "/tests/shared";
import { VAULTPROFILES_USE_CASES } from "../__factories__/use-cases.js";

describe("DownloadVaultDataPack", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: VaultProfilesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<VaultProfilesDDDDependencies>(
      VAULTPROFILES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeDownloadVaultDataPack, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { personId: entity.id };

    await downloadVaultDataPack(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeDownloadVaultDataPack.execute).toHaveBeenCalledWith(expect.objectContaining({ personId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { personId: nonExistentId };
    
    vi.spyOn(mockDeps.executeDownloadVaultDataPack, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      downloadVaultDataPack(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
