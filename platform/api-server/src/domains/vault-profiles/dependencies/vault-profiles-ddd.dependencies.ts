/**
 * VaultProfiles DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: vault-profiles
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * VaultProfiles Domain Module - Composition root
 *
 * DDD: Composition root for vault-profiles domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  DataPackRepositoryAdapter,
  EraseRepositoryAdapter,
  VaultProfileRepositoryAdapter,
} from "@selfsignal/adapters/vault-profiles";
import { getIdGeneratorService } from "@selfsignal/adapters";
import type { AdapterDynamoDBClient } from "@selfsignal/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteCreateVaultProfile,
  ExecuteDownloadVaultDataPack,
  ExecuteEraseVaultProfile,
  ExecuteGetVaultProfile,
  ExecuteListVaultProfiles,
  ExecuteUpdateVaultProfile,
} from "@selfsignal/services/vault-profiles/usecases";
// Ports (interfaces)
import type {
  DataPackRepository,
  EraseRepository,
  VaultProfileRepository,
} from "@selfsignal/services/vault-profiles/ports";

export interface VaultProfilesDomainModule {
  repos: {
    dataPacks: DataPackRepository;
    erases: EraseRepository;
    vaultProfiles: VaultProfileRepository;
  };
  useCases: {
    dataPacks: {
      get: ExecuteDownloadVaultDataPack;
    };
    erases: {
      get: ExecuteEraseVaultProfile;
    };
    vaultProfiles: {
      create: ExecuteCreateVaultProfile;
      get: ExecuteGetVaultProfile;
      list: ExecuteListVaultProfiles;
      update: ExecuteUpdateVaultProfile;
    };
  };
}

export function buildVaultProfilesDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): VaultProfilesDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      dataPacks?: DataPackRepository;
      erases?: EraseRepository;
      vaultProfiles?: VaultProfileRepository;
    } = {};

    obj.dataPacks = new DataPackRepositoryAdapter(dynamoClient);
    obj.erases = new EraseRepositoryAdapter(dynamoClient);
    obj.vaultProfiles = new VaultProfileRepositoryAdapter(dynamoClient);
    return {
      dataPacks: obj.dataPacks!,
      erases: obj.erases!,
      vaultProfiles: obj.vaultProfiles!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    dataPacks: {
      get: new ExecuteDownloadVaultDataPack(executionContext, idGenerator, repos.dataPacks),
    },
    erases: {
      get: new ExecuteEraseVaultProfile(executionContext, idGenerator, repos.erases),
    },
    vaultProfiles: {
      create: new ExecuteCreateVaultProfile(executionContext, idGenerator, repos.vaultProfiles),
      get: new ExecuteGetVaultProfile(executionContext, idGenerator, repos.vaultProfiles),
      list: new ExecuteListVaultProfiles(executionContext, idGenerator, repos.vaultProfiles),
      update: new ExecuteUpdateVaultProfile(executionContext, idGenerator, repos.vaultProfiles),
    },
  };
  return { repos, useCases: useCases as VaultProfilesDomainModule["useCases"] };
}
