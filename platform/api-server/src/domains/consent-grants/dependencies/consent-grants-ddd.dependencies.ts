/**
 * ConsentGrants DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: consent-grants
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ConsentGrants Domain Module - Composition root
 *
 * DDD: Composition root for consent-grants domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  ConsentGrantRepositoryAdapter,
  RenewRepositoryAdapter,
  WithdrawRepositoryAdapter,
} from "@selfsignal/adapters/consent-grants";
import { getIdGeneratorService } from "@selfsignal/adapters";
import type { AdapterDynamoDBClient } from "@selfsignal/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteCreateConsentGrant,
  ExecuteGetConsentGrant,
  ExecuteListConsentGrants,
  ExecuteRenewConsentGrant,
  ExecuteWithdrawConsentGrant,
} from "@selfsignal/services/consent-grants/usecases";
// Ports (interfaces)
import type {
  ConsentGrantRepository,
  RenewRepository,
  WithdrawRepository,
} from "@selfsignal/services/consent-grants/ports";

export interface ConsentGrantsDomainModule {
  repos: {
    consentGrants: ConsentGrantRepository;
    renews: RenewRepository;
    withdraws: WithdrawRepository;
  };
  useCases: {
    consentGrants: {
      create: ExecuteCreateConsentGrant;
      get: ExecuteGetConsentGrant;
      list: ExecuteListConsentGrants;
    };
    renews: {
      get: ExecuteRenewConsentGrant;
    };
    withdraws: {
      get: ExecuteWithdrawConsentGrant;
    };
  };
}

export function buildConsentGrantsDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): ConsentGrantsDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      consentGrants?: ConsentGrantRepository;
      renews?: RenewRepository;
      withdraws?: WithdrawRepository;
    } = {};

    obj.consentGrants = new ConsentGrantRepositoryAdapter(dynamoClient);
    obj.renews = new RenewRepositoryAdapter(dynamoClient);
    obj.withdraws = new WithdrawRepositoryAdapter(dynamoClient);
    return {
      consentGrants: obj.consentGrants!,
      renews: obj.renews!,
      withdraws: obj.withdraws!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    consentGrants: {
      create: new ExecuteCreateConsentGrant(executionContext, idGenerator, repos.consentGrants),
      get: new ExecuteGetConsentGrant(executionContext, idGenerator, repos.consentGrants),
      list: new ExecuteListConsentGrants(executionContext, idGenerator, repos.consentGrants),
    },
    renews: {
      get: new ExecuteRenewConsentGrant(executionContext, idGenerator, repos.renews),
    },
    withdraws: {
      get: new ExecuteWithdrawConsentGrant(executionContext, idGenerator, repos.withdraws),
    },
  };
  return { repos, useCases: useCases as ConsentGrantsDomainModule["useCases"] };
}
