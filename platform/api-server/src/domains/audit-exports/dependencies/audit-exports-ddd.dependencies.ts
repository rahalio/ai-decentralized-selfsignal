/**
 * AuditExports DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: audit-exports
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * AuditExports Domain Module - Composition root
 *
 * DDD: Composition root for audit-exports domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  AttestationRepositoryAdapter,
  AuditExportRepositoryAdapter,
  VerifyRepositoryAdapter,
} from "@selfsignal/adapters/audit-exports";
import { getIdGeneratorService } from "@selfsignal/adapters";
import type { AdapterDynamoDBClient } from "@selfsignal/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteCreateAuditExport,
  ExecuteGetAuditExport,
  ExecuteGetPublicAttestation,
  ExecuteListAuditExports,
  ExecuteVerifyAuditExport,
} from "@selfsignal/services/audit-exports/usecases";
// Ports (interfaces)
import type {
  AttestationRepository,
  AuditExportRepository,
  VerifyRepository,
} from "@selfsignal/services/audit-exports/ports";

export interface AuditExportsDomainModule {
  repos: {
    attestations: AttestationRepository;
    auditExports: AuditExportRepository;
    verifies: VerifyRepository;
  };
  useCases: {
    attestations: {
      get: ExecuteGetPublicAttestation;
    };
    auditExports: {
      create: ExecuteCreateAuditExport;
      get: ExecuteGetAuditExport;
      list: ExecuteListAuditExports;
    };
    verifies: {
      get: ExecuteVerifyAuditExport;
    };
  };
}

export function buildAuditExportsDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): AuditExportsDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      attestations?: AttestationRepository;
      auditExports?: AuditExportRepository;
      verifies?: VerifyRepository;
    } = {};

    obj.attestations = new AttestationRepositoryAdapter(dynamoClient);
    obj.auditExports = new AuditExportRepositoryAdapter(dynamoClient);
    obj.verifies = new VerifyRepositoryAdapter(dynamoClient);
    return {
      attestations: obj.attestations!,
      auditExports: obj.auditExports!,
      verifies: obj.verifies!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    attestations: {
      get: new ExecuteGetPublicAttestation(executionContext, idGenerator, repos.attestations),
    },
    auditExports: {
      create: new ExecuteCreateAuditExport(executionContext, idGenerator, repos.auditExports),
      get: new ExecuteGetAuditExport(executionContext, idGenerator, repos.auditExports),
      list: new ExecuteListAuditExports(executionContext, idGenerator, repos.auditExports),
    },
    verifies: {
      get: new ExecuteVerifyAuditExport(executionContext, idGenerator, repos.verifies),
    },
  };
  return { repos, useCases: useCases as AuditExportsDomainModule["useCases"] };
}
