/**
 * Purposes DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: purposes
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Purposes Domain Module - Composition root
 *
 * DDD: Composition root for purposes domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  PurposeRepositoryAdapter,
} from "@selfsignal/adapters/purposes";
import { getIdGeneratorService } from "@selfsignal/adapters";
import type { AdapterDynamoDBClient } from "@selfsignal/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteCreatePurpose,
  ExecuteGetPurpose,
  ExecuteListPurposes,
  ExecuteUpdatePurpose,
} from "@selfsignal/services/purposes/usecases";
// Ports (interfaces)
import type {
  PurposeRepository,
} from "@selfsignal/services/purposes/ports";

export interface PurposesDomainModule {
  repos: {
    purposes: PurposeRepository;
  };
  useCases: {
    purposes: {
      create: ExecuteCreatePurpose;
      get: ExecuteGetPurpose;
      list: ExecuteListPurposes;
      update: ExecuteUpdatePurpose;
    };
  };
}

export function buildPurposesDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): PurposesDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      purposes?: PurposeRepository;
    } = {};

    obj.purposes = new PurposeRepositoryAdapter(dynamoClient);
    return {
      purposes: obj.purposes!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    purposes: {
      create: new ExecuteCreatePurpose(executionContext, idGenerator, repos.purposes),
      get: new ExecuteGetPurpose(executionContext, idGenerator, repos.purposes),
      list: new ExecuteListPurposes(executionContext, idGenerator, repos.purposes),
      update: new ExecuteUpdatePurpose(executionContext, idGenerator, repos.purposes),
    },
  };
  return { repos, useCases: useCases as PurposesDomainModule["useCases"] };
}
