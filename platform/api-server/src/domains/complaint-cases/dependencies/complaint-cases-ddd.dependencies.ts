/**
 * ComplaintCases DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: complaint-cases
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ComplaintCases Domain Module - Composition root
 *
 * DDD: Composition root for complaint-cases domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  CloseRepositoryAdapter,
  ComplaintCaseRepositoryAdapter,
  EscalateRepositoryAdapter,
} from "@selfsignal/adapters/complaint-cases";
import { getIdGeneratorService } from "@selfsignal/adapters";
import type { AdapterDynamoDBClient } from "@selfsignal/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteCloseComplaintCase,
  ExecuteEscalateComplaintCase,
  ExecuteGetComplaintCase,
  ExecuteListComplaintCases,
  ExecuteOpenComplaintCase,
} from "@selfsignal/services/complaint-cases/usecases";
// Ports (interfaces)
import type {
  CloseRepository,
  ComplaintCaseRepository,
  EscalateRepository,
} from "@selfsignal/services/complaint-cases/ports";

export interface ComplaintCasesDomainModule {
  repos: {
    closes: CloseRepository;
    complaintCases: ComplaintCaseRepository;
    escalates: EscalateRepository;
  };
  useCases: {
    closes: {
      get: ExecuteCloseComplaintCase;
    };
    complaintCases: {
      get: ExecuteGetComplaintCase;
      list: ExecuteListComplaintCases;
    };
    escalates: {
      get: ExecuteEscalateComplaintCase;
    };
  };
}

export function buildComplaintCasesDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): ComplaintCasesDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      closes?: CloseRepository;
      complaintCases?: ComplaintCaseRepository;
      escalates?: EscalateRepository;
    } = {};

    obj.closes = new CloseRepositoryAdapter(dynamoClient);
    obj.complaintCases = new ComplaintCaseRepositoryAdapter(dynamoClient);
    obj.escalates = new EscalateRepositoryAdapter(dynamoClient);
    return {
      closes: obj.closes!,
      complaintCases: obj.complaintCases!,
      escalates: obj.escalates!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    closes: {
      get: new ExecuteCloseComplaintCase(executionContext, idGenerator, repos.closes),
    },
    complaintCases: {
      get: new ExecuteGetComplaintCase(executionContext, idGenerator, repos.complaintCases),
      list: new ExecuteListComplaintCases(executionContext, idGenerator, repos.complaintCases),
    },
    escalates: {
      get: new ExecuteEscalateComplaintCase(executionContext, idGenerator, repos.escalates),
    },
  };
  return { repos, useCases: useCases as ComplaintCasesDomainModule["useCases"] };
}
