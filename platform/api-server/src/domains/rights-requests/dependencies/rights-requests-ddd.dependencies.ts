/**
 * RightsRequests DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: rights-requests
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * RightsRequests Domain Module - Composition root
 *
 * DDD: Composition root for rights-requests domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  FulfilRepositoryAdapter,
  RightsRequestRepositoryAdapter,
} from "@selfsignal/adapters/rights-requests";
import { getIdGeneratorService } from "@selfsignal/adapters";
import type { AdapterDynamoDBClient } from "@selfsignal/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteCreateRightsRequest,
  ExecuteFulfilRightsRequest,
  ExecuteGetRightsRequest,
  ExecuteListRightsRequests,
} from "@selfsignal/services/rights-requests/usecases";
// Ports (interfaces)
import type {
  FulfilRepository,
  RightsRequestRepository,
} from "@selfsignal/services/rights-requests/ports";

export interface RightsRequestsDomainModule {
  repos: {
    fulfils: FulfilRepository;
    rightsRequests: RightsRequestRepository;
  };
  useCases: {
    fulfils: {
      get: ExecuteFulfilRightsRequest;
    };
    rightsRequests: {
      create: ExecuteCreateRightsRequest;
      get: ExecuteGetRightsRequest;
      list: ExecuteListRightsRequests;
    };
  };
}

export function buildRightsRequestsDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): RightsRequestsDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      fulfils?: FulfilRepository;
      rightsRequests?: RightsRequestRepository;
    } = {};

    obj.fulfils = new FulfilRepositoryAdapter(dynamoClient);
    obj.rightsRequests = new RightsRequestRepositoryAdapter(dynamoClient);
    return {
      fulfils: obj.fulfils!,
      rightsRequests: obj.rightsRequests!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    fulfils: {
      get: new ExecuteFulfilRightsRequest(executionContext, idGenerator, repos.fulfils),
    },
    rightsRequests: {
      create: new ExecuteCreateRightsRequest(executionContext, idGenerator, repos.rightsRequests),
      get: new ExecuteGetRightsRequest(executionContext, idGenerator, repos.rightsRequests),
      list: new ExecuteListRightsRequests(executionContext, idGenerator, repos.rightsRequests),
    },
  };
  return { repos, useCases: useCases as RightsRequestsDomainModule["useCases"] };
}
