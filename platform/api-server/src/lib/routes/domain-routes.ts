/**
 * Domain routes – register all domain route modules.
 *
 * AUTO-GENERATED by codegen (api_server_domain_routes). Do not edit by hand.
 * Rescans platform/api-server/src/domains and includes only domains that have
 * routes/index.ts and dependencies/{domain}-ddd.dependencies.ts.
 * Extension excludes/imports from x-codegen.api-server.extensions in OpenAPI YAML.
 */

import type { FastifyInstance } from "fastify";
import { registerDomainFromModule } from "./route-registration.js";

// Static imports so the bundle includes all domain route modules
import * as auditExportsRoutes from "../../domains/audit-exports/routes/index.js";
import * as campaignsRoutes from "../../domains/campaigns/routes/index.js";
import * as complaintCasesRoutes from "../../domains/complaint-cases/routes/index.js";
import * as consentGrantsRoutes from "../../domains/consent-grants/routes/index.js";
import * as coverageReportsRoutes from "../../domains/coverage-reports/routes/index.js";
import * as identityRoutes from "../../domains/identity/routes/index.js";
import * as integrationsRoutes from "../../domains/integrations/routes/index.js";
import * as purposesRoutes from "../../domains/purposes/routes/index.js";
import * as rightsRequestsRoutes from "../../domains/rights-requests/routes/index.js";
import * as signalActivationsRoutes from "../../domains/signal-activations/routes/index.js";
import * as vaultProfilesRoutes from "../../domains/vault-profiles/routes/index.js";

const DOMAIN_MODULES: Array<{
  domain: string;
  mod: Record<string, unknown>;
  exclude?: string[];
}> = [
  { domain: "audit-exports", mod: auditExportsRoutes },
  { domain: "campaigns", mod: campaignsRoutes },
  { domain: "complaint-cases", mod: complaintCasesRoutes },
  { domain: "consent-grants", mod: consentGrantsRoutes },
  { domain: "coverage-reports", mod: coverageReportsRoutes },
  {
    domain: "identity", mod: identityRoutes,
    exclude: ["authRoutes", "loginRoutes", "logoutRoutes", "signupRoutes", "sessionRoutes", "refreshRoutes", "tokenRoutes", "profileRoutes"],
  },
  { domain: "integrations", mod: integrationsRoutes },
  { domain: "purposes", mod: purposesRoutes },
  { domain: "rights-requests", mod: rightsRequestsRoutes },
  { domain: "signal-activations", mod: signalActivationsRoutes },
  { domain: "vault-profiles", mod: vaultProfilesRoutes },
];

export async function registerAllDomainRoutes(
  fastify: FastifyInstance,
): Promise<void> {
  console.log("[DEBUG] Registering domain routes...");
  try {
    for (const { domain, mod, exclude } of DOMAIN_MODULES) {
      await registerDomainFromModule(fastify, domain, mod, exclude ?? []);
    }
    console.log("[DEBUG] ✅ All domain routes registered");
  } catch (error: unknown) {
    const err = error instanceof Error ? error : new Error(String(error));
    console.error("[DEBUG] ❌ Error registering domain routes:", err.message);
    if (err.stack) console.error(err.stack);
    throw error;
  }
}
