/**
 * ExecuteListIntegrations - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: integrations
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteListIntegrations Use Case
 *
 * DDD: Application use case for list Integration.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ListIntegrationsInput, ListIntegrationsOutput } from "../dto/integration.dto";
import type { ExecutionContextService, IdGeneratorService } from "@selfsignal/services/_shared/index.js";
import type { IntegrationRepository } from "../ports";

export class ExecuteListIntegrations {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly integration: IntegrationRepository
  ) {}

  async execute(input: ListIntegrationsInput): Promise<ListIntegrationsOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.intId();

    // All field names extracted from OpenAPI spec: none
    // List entities via repository
    const result = await this.integration.listIntegrations({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return result as ListIntegrationsOutput;
  }
}
