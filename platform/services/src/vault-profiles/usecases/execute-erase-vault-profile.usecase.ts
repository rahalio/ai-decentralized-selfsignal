/**
 * ExecuteEraseVaultProfile - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: vault-profiles
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteEraseVaultProfile Use Case
 *
 * DDD: Application use case for get Erase.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { EraseVaultProfileInput, EraseVaultProfileOutput } from "../dto/erase.dto";
import type { ExecutionContextService, IdGeneratorService } from "@selfsignal/services/_shared/index.js";
import type { EraseRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteEraseVaultProfile {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly erase: EraseRepository
  ) {}

  async execute(input: EraseVaultProfileInput): Promise<EraseVaultProfileOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.vpfId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).personId) {
      throw new ValidationError("personId is required");
    }
    const personId = (input as any).personId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.erase.eraseVaultProfile({ personId: personId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${personId}`);
    }

    return entity as EraseVaultProfileOutput;
  }
}
