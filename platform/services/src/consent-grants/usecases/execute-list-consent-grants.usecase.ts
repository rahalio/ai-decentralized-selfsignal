/**
 * ExecuteListConsentGrants - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: consent-grants
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteListConsentGrants Use Case
 *
 * DDD: Application use case for list ConsentGrant.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ListConsentGrantsInput, ListConsentGrantsOutput } from "../dto/consent-grant.dto";
import type { ExecutionContextService, IdGeneratorService } from "@selfsignal/services/_shared/index.js";
import type { ConsentGrantRepository } from "../ports";

export class ExecuteListConsentGrants {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly consentGrant: ConsentGrantRepository
  ) {}

  async execute(input: ListConsentGrantsInput): Promise<ListConsentGrantsOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.cgrId();

    // All field names extracted from OpenAPI spec: none
    // List entities via repository
    const result = await this.consentGrant.listConsentGrants({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return result as ListConsentGrantsOutput;
  }
}
