/**
 * ExecuteGetCoverageReport - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: coverage-reports
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetCoverageReport Use Case
 *
 * DDD: Application use case for get CoverageReport.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetCoverageReportInput, GetCoverageReportOutput } from "../dto/coverage-report.dto";
import type { ExecutionContextService, IdGeneratorService } from "@selfsignal/services/_shared/index.js";
import type { CoverageReportRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetCoverageReport {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly coverageReport: CoverageReportRepository
  ) {}

  async execute(input: GetCoverageReportInput): Promise<GetCoverageReportOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.cvrId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).reportId) {
      throw new ValidationError("reportId is required");
    }
    const reportId = (input as any).reportId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.coverageReport.getCoverageReport({ reportId: reportId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${reportId}`);
    }

    return entity as GetCoverageReportOutput;
  }
}
